---
must_haves:
  truths:
    - Deep Forge keeps AI conversation as the center workspace.
    - Right side becomes a visual chamber with Reference / Prototype / Compare tabs.
    - Prototype preview lives in the right chamber, not in the chat stream or center area.
    - Prototype generation can run from the current PRD node and its polished chat context.
    - Reference images uploaded in the chat remain visible as evidence cards after sending.
  artifacts:
    - src/pages/ForgePage.tsx
    - src/components/map/ForgeChat.tsx
    - src/components/state/PrototypeBoard.tsx
    - .planning/quick/260527-x9l-deep-forge-ai-tab/260527-x9l-SUMMARY.md
    - .planning/quick/260527-x9l-deep-forge-ai-tab/260527-x9l-VERIFICATION.md
---

# Quick Task 260527-x9l Plan

## Task 1: Reframe Deep Forge Layout Around AI Conversation

files:
- `src/pages/ForgePage.tsx`
- `src/components/map/ForgeChat.tsx`

action:
- Keep `ForgeNodePanel` on the left.
- Keep the AI message list and input as the central working area.
- Move all visual browsing, reference upload evidence, prototype preview, and comparison into a right-side visual chamber.

verify:
- ForgePage still passes node, messages, send, confirm, and back flows.
- ForgeChat no longer treats the right column as a simple static reference panel.

done:
- Center is AI collaboration; right is visual output/evidence.

## Task 2: Add Visual Chamber Tabs

files:
- `src/components/map/ForgeChat.tsx`
- `src/components/state/PrototypeBoard.tsx`

action:
- Add tabs: `参考图`, `原型`, `对比`.
- Reference tab shows uploaded/sent reference image cards and the upload controls.
- Prototype tab hosts the phone prototype preview, history, and iteration controls.
- Compare tab shows selected reference image beside the current prototype preview.

verify:
- Empty reference/prototype/compare states are clear.
- Selecting/uploading images moves users naturally into the reference tab.
- Generating a prototype moves users to the prototype tab.

done:
- Right side can switch between evidence, generated mobile prototype, and comparison.

## Task 3: Wire Node Prototype Generation

files:
- `src/pages/ForgePage.tsx`
- `src/components/map/ForgeChat.tsx`

action:
- Build a temporary `UXRequirementState` from the current PRD node and its node chat transcript.
- Call existing `/api/prototype` through `generatePrototype()`.
- Use existing `prototypeHtml`, `prototypeHistory`, `setPrototypeHtml`, and `restorePrototypeVersion` store fields.
- Support generation and instruction-based iteration from the right prototype tab.

verify:
- TypeScript passes.
- `npm run build` passes.

done:
- Deep Forge can generate and iterate the right-side phone prototype preview.

## Task 4: GSD Closeout

files:
- `.planning/quick/260527-x9l-deep-forge-ai-tab/260527-x9l-SUMMARY.md`
- `.planning/quick/260527-x9l-deep-forge-ai-tab/260527-x9l-VERIFICATION.md`
- `.planning/STATE.md`

action:
- Run verification.
- Write summary and verification artifacts.
- Update quick task table.

verify:
- GSD files exist and STATE has a row for 260527-x9l.

done:
- Iteration is tracked.
