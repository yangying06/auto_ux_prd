---
phase: quick-260602-h4b-4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/appStore.ts
  - src/components/state/PrototypeBoard.tsx
  - src/components/map/ForgeChat.tsx
  - src/pages/ForgePage.tsx
autonomous: true
requirements:
  - QUICK-260602-H4B
must_haves:
  truths:
    - "打磨界面的原型历史版本最多只保留 4 个可恢复版本。"
    - "用户能在原型历史版本控件旁边点击一个明确的按钮来清空历史版本。"
    - "清空历史版本不会清空当前已选原型预览。"
  artifacts:
    - path: "src/store/appStore.ts"
      provides: "原型历史版本上限与清空历史版本 store action"
      contains: "PROTOTYPE_HISTORY_LIMIT = 4"
    - path: "src/components/state/PrototypeBoard.tsx"
      provides: "历史版本下拉框旁的清空历史按钮"
      contains: "清空历史"
    - path: "src/components/map/ForgeChat.tsx"
      provides: "将清空历史事件从视觉舱传给原型面板"
    - path: "src/pages/ForgePage.tsx"
      provides: "从 Zustand 读取 clearPrototypeHistory 并传给 ForgeChat"
  key_links:
    - from: "src/components/state/PrototypeBoard.tsx"
      to: "src/store/appStore.ts"
      via: "onClearHistory prop passed through ForgeChat and ForgePage"
      pattern: "onClearHistory"
---

<objective>
让 Deep Forge 打磨界面的原型历史版本更轻量：历史版本最多保留 4 个，并在历史版本控件旁提供“清空历史”按钮。

Purpose: 避免历史版本无限堆积造成选择噪音，同时给用户一个快速清理历史版本的显式操作。
Output: Zustand 历史保留上限更新、原型面板清空历史按钮、ForgePage/ForgeChat 事件透传。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@src/store/appStore.ts
@src/components/state/PrototypeBoard.tsx
@src/components/map/ForgeChat.tsx
@src/pages/ForgePage.tsx

<interfaces>
Existing store contract in `src/store/appStore.ts`:
```ts
export interface PrototypeVersion {
  id: string
  label: string
  html: string
  createdAt: string
  mode: 'create' | 'update' | 'restore'
  note: string | null
}

interface AppStoreState {
  prototypeHtml: string | null
  prototypeHistory: PrototypeVersion[]
  recordPrototypeHistory: (html: string, meta?: PrototypeVersionMeta) => void
  restorePrototypeVersion: (id: string) => void
  clearPrototypeHistory: () => void
}
```

Existing component contract in `src/components/state/PrototypeBoard.tsx`:
```ts
interface PrototypeBoardProps {
  html: string | null
  history: PrototypeVersion[]
  isLoading: boolean
  onIterate: (instruction: string) => void
  onRestore: (id: string) => void
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cap prototype history to 4 retained versions</name>
  <files>src/store/appStore.ts</files>
  <behavior>
    - When `setPrototypeHtml()` records a new version, `prototypeHistory.length` never exceeds 4.
    - When `recordPrototypeHistory()` records a new version, `prototypeHistory.length` never exceeds 4.
    - When persisted v3-v6 state is migrated, existing `prototypeHistory` is normalized to at most 4 entries so old localStorage cannot rehydrate 8 versions.
  </behavior>
  <action>Change `PROTOTYPE_HISTORY_LIMIT` from 8 to 4. Update migration branches that carry forward `previous.prototypeHistory` so they only keep the first 4 entries when the value is an array. Do not bump `STORAGE_VERSION` unless required by TypeScript; this is a retention policy change, not a schema change.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>All code paths that add or migrate prototype history cap the retained list at 4 versions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add clear-history action to the prototype board UI</name>
  <files>src/components/state/PrototypeBoard.tsx, src/components/map/ForgeChat.tsx, src/pages/ForgePage.tsx</files>
  <behavior>
    - `PrototypeBoard` renders a “清空历史” button immediately beside the historical version selector.
    - The button is disabled when there is no history or the prototype is currently loading.
    - Clicking the button clears only `prototypeHistory`; the current `prototypeHtml` preview remains visible.
  </behavior>
  <action>Add `onClearHistory: () => void` to `PrototypeBoardProps`, render a small button next to the existing history `<select>` in the top bar, and call `onClearHistory` from that button. Add matching `onClearPrototypeHistory` prop to `ForgeChatProps`, pass it into `PrototypeBoard`, then read `clearPrototypeHistory` from `useAppStore` in `ForgePage` and pass it to `ForgeChat`. In `src/store/appStore.ts`, adjust `clearPrototypeHistory` if needed so it clears `prototypeHistory` only and does not set `prototypeHtml` to `null`; this preserves the current preview while removing restore history.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>The Deep Forge prototype panel shows a clear-history button beside the selector, clears the restore list, and keeps the current preview intact.</done>
</task>

</tasks>

<verification>
Run `npm run typecheck`. Optional manual check: open a node in Deep Forge, generate/iterate prototypes until history exists, confirm only 4 history items are visible/available, then click “清空历史” and verify the selector is empty while the current preview remains.
</verification>

<success_criteria>
- Prototype history retention limit is 4 everywhere history is created or migrated.
- A clear-history button appears next to the historical version selector in the prototype panel.
- Clearing history does not erase the current prototype preview or current variant selection.
- `npm run typecheck` passes.
</success_criteria>

<output>
After completion, create `.planning/quick/260602-h4b-4/260602-h4b-SUMMARY.md`
</output>
