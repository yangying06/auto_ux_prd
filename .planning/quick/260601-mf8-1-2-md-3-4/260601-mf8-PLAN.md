---
phase: quick-260601-mf8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/appStore.ts
  - src/components/map/ForgeChat.tsx
  - src/pages/ForgePage.tsx
  - server/index.ts
autonomous: true
requirements:
  - QUICK-MF8-01
  - QUICK-MF8-02
  - QUICK-MF8-03
  - QUICK-MF8-04
must_haves:
  truths:
    - "用户可以在 Deep Forge 单节点打磨界面手动清空当前节点聊天记录。"
    - "AI 回复在聊天记录中以精简后的 Markdown 正文渲染，不展示末尾 JSON 或大段原始回包。"
    - "用户在聊天中提出并被 AI 采纳的迭代修改内容会合并到当前节点打磨文档，左侧文档预览和后续导出使用更新后的节点内容。"
    - "用户发送过的参考图刷新页面后仍保留在对应节点的聊天记录和参考图图库中，只有手动清空聊天时才删除。"
  artifacts:
    - path: "src/store/appStore.ts"
      provides: "节点聊天持久化、图片保留、当前节点清空动作"
      contains: "nodeChats"
    - path: "src/components/map/ForgeChat.tsx"
      provides: "清空按钮、Markdown 消息渲染、持久化图片图库展示"
      contains: "onClearChat"
    - path: "src/pages/ForgePage.tsx"
      provides: "发送后把 nodePatch/摘要合并到当前 PrdNode，并把清空动作传给 ForgeChat"
      contains: "applyNodePolish"
    - path: "server/index.ts"
      provides: "节点聊天提示约束：回复精简、Markdown 正文、每轮可返回 nodePatch"
      contains: "nodeChatSystemPrompt"
  key_links:
    - from: "src/components/map/ForgeChat.tsx"
      to: "src/store/appStore.ts"
      via: "onClearChat prop calls clearNodeChat(nodeId)"
      pattern: "onClearChat"
    - from: "src/store/appStore.ts"
      to: "localStorage gameux-promptforge-state"
      via: "partialize persists nodeChats without stripping image ContentBlock data"
      pattern: "nodeChats: persistableNodeChats"
    - from: "server/index.ts"
      to: "src/pages/ForgePage.tsx"
      via: "sendNodeChatMessage returns reply + nodePatch; ForgePage applies nodePatch immediately"
      pattern: "nodePatch"
---

<objective>
Improve the Deep Forge polishing interface so chat history is controllable, readable, persistent with reference images, and materially updates the corresponding polished document.

Purpose: Users should be able to treat Deep Forge conversations as durable refinement evidence and document edits, not transient raw AI logs.
Output: One focused implementation touching the Zustand store, Forge chat UI, Forge page wiring, and node-chat server prompt/response constraints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/store/appStore.ts
@src/components/map/ForgeChat.tsx
@src/pages/ForgePage.tsx
@src/lib/api.ts
@server/index.ts

<interfaces>
Existing contracts to preserve:

From src/types/chat.ts:
```typescript
export interface TextBlock { type: 'text'; text: string }
export interface ImageBlock {
  type: 'image'
  source: { type: 'base64'; media_type: 'image/jpeg' | 'image/png' | 'image/gif' | 'image/webp'; data: string }
}
export type ContentBlock = TextBlock | ImageBlock
export interface ChatMessage { role: 'user' | 'assistant'; content: string | ContentBlock[] }
```

From src/store/appStore.ts:
```typescript
nodeChats: Record<string, ChatMessage[]>
appendNodeMessage: (nodeId: string, msg: ChatMessage) => void
clearNodeChat: (nodeId: string) => void
applyNodePolish: (nodeId: string, patch: { summary?: string | null; content?: string | null; techNotes?: string | null }) => void
```

From src/lib/api.ts:
```typescript
export interface NodeChatResponse {
  reply: string
  nodeComplete: boolean
  nodePatch?: { summary?: string | null; content?: string | null; techNotes?: string | null } | null
}
export function sendNodeChatMessage(baseUrl: string, nodeId: string, messages: ChatMessage[], tree: Record<string, PrdNode>)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist node chat images until manual clear</name>
  <files>src/store/appStore.ts</files>
  <behavior>
    - Test 1: A node chat message whose content contains image ContentBlock is persisted and rehydrated with base64 `source.data` intact.
    - Test 2: `clearNodeChat(nodeId)` removes that node's messages and images while leaving other nodes' chats intact.
    - Test 3: Existing text-only persisted chats still migrate without data loss.
  </behavior>
  <action>Update `persistableMessage()` / `persistableNodeChats()` so persisted `nodeChats` keep full `ContentBlock[]` content, including base64 image data, instead of converting images to `[图片附件: ...]` text. Keep the existing slice limit if needed, but do not strip image blocks. Ensure the only deletion path for node chat images is `clearNodeChat(nodeId)` or full decomposition reset, matching the user requirement that images remain until manual clearing. If storage version must change, bump `STORAGE_VERSION` minimally and update `migrate` to carry forward existing v3-v7 fields; do not wipe `prdTree` or `nodeChats` during migration.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Refreshing the app preserves sent reference images in `nodeChats`; clearing one node chat removes only that node's messages/images.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add clear control and Markdown chat rendering</name>
  <files>src/components/map/ForgeChat.tsx, src/pages/ForgePage.tsx</files>
  <behavior>
    - Test 1: Clicking “清空聊天” clears the current node's chat history and reference gallery, then the page can show the normal welcome message again if appropriate.
    - Test 2: Assistant replies containing Markdown headings, lists, bold text, inline code, and fenced code blocks render as readable formatted content instead of raw plain text.
    - Test 3: Markdown rendering does not use `dangerouslySetInnerHTML` and does not introduce a new external dependency unless already present in `package.json`.
  </behavior>
  <action>Add an `onClearChat` prop to `ForgeChatProps`; wire it in `ForgePage` to `clearNodeChat(nodeId)` from the Zustand store. Add a compact clear button near the chat toolbar/status row with copy such as `清空聊天`; disable while sending if necessary. The clear action should call the store action only, not mutate `attachments` beyond clearing any pending unsent images if that is the current UX expectation. Replace `renderMessageContent()` text rendering with a small internal Markdown renderer that supports the formats actually needed in AI replies: `#`/`##`/`###` headings, unordered/ordered lists, paragraphs, bold, inline code, and fenced code blocks. Keep it simple, component-based, and escaped by React text rendering; do not add `react-markdown` because it is not in `package.json` and this quick task has no research phase. Continue rendering image blocks as thumbnails below the rendered text blocks.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Current-node chat has a visible clear button; AI replies are formatted as Markdown; sent image thumbnails still appear inside chat bubbles and in the reference tab.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Keep replies concise and merge iterative refinements into node documents</name>
  <files>server/index.ts, src/pages/ForgePage.tsx, src/lib/api.ts</files>
  <behavior>
    - Test 1: `/api/node-chat` returns a concise Markdown `reply` without exposing suffix JSON in the chat bubble.
    - Test 2: When the AI returns `nodePatch` after an iteration, `ForgePage.handleSend()` immediately applies it through `applyNodePolish()` so `ForgeNodePanel` shows the updated document content.
    - Test 3: If the AI has not marked `nodeComplete`, the server is still allowed to return a partial `nodePatch` for accepted user iteration changes, not only at final completion.
  </behavior>
  <action>Revise the `nodeChatSystemPrompt` in `server/index.ts` so assistant chat replies are concise Markdown summaries for the user, not full rewritten documents. Keep or strengthen the existing JSON suffix parsing contract, but change the instruction so the model may append `nodePatch` whenever a user-provided iteration should be merged into the current document, even if `nodeComplete` is false. In `src/pages/ForgePage.tsx`, keep the existing `applyNodePolish(nodeId, mergePolishPatch(...))` path but ensure it runs for partial patches as well; if needed, adjust `mergePolishPatch()` so patch content is treated as the current Deep Forge polished document section and does not duplicate old Deep Forge sections. In `src/lib/api.ts`, only adjust TypeScript response typing if the server contract needs `nodePatch` with `nodeComplete: false`; do not change the endpoint path or request payload.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>New chat iterations update the corresponding `PrdNode.content`/summary/techNotes via `nodePatch`, while the visible assistant reply remains short and Markdown-formatted.</done>
</task>

</tasks>

<verification>
Run `npm run typecheck`. Then manually smoke test in the Vite app: open a Deep Forge node, send a Markdown-producing prompt, upload/send a reference image, refresh, confirm the image remains in chat and reference gallery, send an iteration that changes the document, confirm the left document preview updates, then click `清空聊天` and confirm this node's chat/gallery clears.
</verification>

<success_criteria>
- Current node chat can be cleared by the user from the Deep Forge UI.
- Assistant chat replies are concise and readable as Markdown.
- Accepted chat iteration changes are merged into the current node's polished document content for preview/export.
- Sent reference images survive refresh in both chat history and reference gallery until the user clears that node's chat.
- `npm run typecheck` passes.
</success_criteria>

<output>
After completion, create `.planning/quick/260601-mf8-1-2-md-3-4/260601-mf8-SUMMARY.md`.
</output>
