---
phase: quick-260602-f4c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/index.ts
  - src/components/map/TreeCanvas.tsx
  - src/components/map/NodeCard.tsx
autonomous: true
requirements:
  - QUICK-260602-F4C
---

# Quick Task 260602-f4c Plan

## Objective

修正 PRD 拆解策略，让导图结构变为：单个原文目录根节点 -> AI 页面节点 -> 有原文依据的 MVC 子节点。

## Locked Decisions

- 原文标题/目录使用单个根节点展示，页面节点挂在该根节点下。
- MVC 子节点有内容才生成，不强制凑齐 model、ctrl、view。
- 节点内容采用“引用+整理”：保留原文位置、关键原文摘录、整理说明、需澄清点。

## Tasks

1. Update [server/index.ts](../../../server/index.ts) decomposition flow:
   - Build a local source-outline root node from Markdown headings.
   - Attach AI page nodes under the source-outline root.
   - Expand each page into only evidence-backed model/ctrl/view children.
   - Preserve natural progress behavior for large PRDs.
2. Update map labels in [TreeCanvas.tsx](../../../src/components/map/TreeCanvas.tsx) and [NodeCard.tsx](../../../src/components/map/NodeCard.tsx) to reflect 原文目录 -> 页面节点 -> MVC 文档.

## Verification

- `npm run typecheck:server`
- `npm run typecheck`
- Source checks for `SOURCE_OUTLINE_ROOT`, `原文目录`, `model`, `ctrl`, `view`, `关键原文摘录`
- UI label checks for `页面节点`, `MVC`, `原文目录`
