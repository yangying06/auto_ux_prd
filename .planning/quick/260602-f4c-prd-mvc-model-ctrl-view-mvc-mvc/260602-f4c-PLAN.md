---
phase: quick-260602-f4c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/index.ts
  - src/components/map/TreeCanvas.tsx
  - src/components/map/NodeCard.tsx
autonomous: true
requirements:
  - QUICK-260602-F4C
must_haves:
  truths:
    - "导入 PRD 后，导图最左侧先出现单个原文目录根节点，content 列出原文 Markdown 标题/目录。"
    - "页面节点由 AI 根据完整原文生成并挂在原文目录根节点下，不允许本地根据标题伪造页面节点。"
    - "每个页面节点下只生成有原文依据的 MVC 子节点：model、ctrl、view 有内容才出现，缺失项只在已有节点中标注需澄清。"
    - "节点正文采用引用+整理：包含原文位置、关键原文摘录、结构化整理和需澄清点。"
    - "大 PRD 拆解仍保持现有轮询、心跳和分支并发进度反馈，不回退为静默等待。"
  artifacts:
    - path: "server/index.ts"
      provides: "PRD 拆解提示词、AI 工具 schema、根目录节点组装、MVC 分支展开与导出索引适配"
      contains: "SOURCE_OUTLINE_ROOT"
    - path: "src/components/map/TreeCanvas.tsx"
      provides: "导图层级标签对齐：原文目录 -> 页面节点 -> MVC 文档"
      contains: "MVC"
    - path: "src/components/map/NodeCard.tsx"
      provides: "根节点卡片显示为原文目录语义"
      contains: "原文目录"
  key_links:
    - from: "server/index.ts buildSourceOutlineRootNode"
      to: "server/index.ts runDecompositionJob"
      via: "local source-outline root is merged before AI page nodes"
      pattern: "buildSourceOutlineRootNode.*mergeSessionNodes"
    - from: "server/index.ts decomposeL1"
      to: "server/index.ts decomposeBranch"
      via: "AI page nodes under source root become branch expansion inputs"
      pattern: "parentId.*SOURCE_OUTLINE_ROOT"
    - from: "server/index.ts decompositionBranchSystemPrompt"
      to: "src/components/map/TreeCanvas.tsx"
      via: "MVC children render as depth-2 descendants under page nodes"
      pattern: "model|ctrl|view|MVC"
---

<objective>
修正 PRD 拆解策略，让导图从“粗略文档包树”变为“原文目录根节点 -> 页面节点 -> MVC 子节点”的保真结构。

Purpose: 大文档拆解必须更尊重原文，用户能从导图上看见原文目录依据，并在每个页面下继续进入有依据的 model / ctrl / view 子文档。
Output: 一个后端优先的拆解策略修正，附带最小 UI 标签对齐。
</objective>

<execution_context>
@C:/Users/13760/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/13760/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260602-f4c-prd-mvc-model-ctrl-view-mvc-mvc/260602-f4c-CONTEXT.md
@server/index.ts
@src/types/prdNode.ts
@src/components/map/TreeCanvas.tsx
@src/components/map/NodeCard.tsx

<locked_decisions>
D-01 原文目录前置节点：使用单个根节点承载原文目录，导图根节点为“原文目录/PRD 原文目录”，content 列出 Markdown 标题；页面节点挂在该根节点下面。
D-02 MVC 子节点粒度：MVC 子节点不强制固定三节点；只为原文明确涉及的 model、ctrl、view 部分生成子节点，避免空节点膨胀。缺失信息可在页面节点或已有 MVC 节点中标注“需澄清”。
D-03 原文保真策略：节点内容采用“引用+整理”：保留原文依据、行号/标题位置、关键原文摘录，再做简短结构化整理。
</locked_decisions>

<interfaces>
Existing PrdNode contract from src/types/prdNode.ts:
```typescript
export interface PrdNode {
  id: string
  parentId: string | null
  label: string
  summary: string
  content: string
  type: 'module' | 'feature' | 'ui'
  status: 'pending' | 'done'
  level: number
  order: number
  needsPolish: boolean
  extractedFrom: string | null
  techNotes: string | null
  children: string[]
  docPath?: string | null
  audience?: PrdNodeAudience | null
  handoffGoal?: string | null
  qualityGate?: string | null
}
```
Use the existing contract unless implementation proves a tiny optional field is necessary. The root outline node can be represented as `type: 'module'`, `parentId: null`, `level: 0`, `docPath: null`, `needsPolish: false`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rework backend decomposition into source-outline root, AI pages, and evidence-backed MVC children</name>
  <files>server/index.ts</files>
  <behavior>
    - Given Markdown with headings, the decomposition session includes exactly one local source-outline root node before AI-derived nodes; its content lists heading titles and line numbers, and it is the only locally-created structural node.
    - AI top-level decomposition returns page/screen nodes whose `parentId` is the source-outline root ID; local Markdown headings are prompt context only and never become page nodes.
    - Branch decomposition for each page returns only MVC children with original-source evidence: model for data/state/config, ctrl for interactions/logic/API/control flow, view for layout/visual/copy/animation. Missing MVC categories are not emitted as empty nodes.
    - Every generated page or MVC node content asks Claude for “原文位置 / 关键原文摘录 / 整理说明 / 需澄清点” so output is quote+organize per D-03.
  </behavior>
  <action>
    Update `server/index.ts` surgically around the existing decomposition section only.

    Implement D-01 by adding a stable source outline root helper, for example `SOURCE_OUTLINE_ROOT_ID` and `buildSourceOutlineRootNode(mdText): PrdNode`. The helper may use the existing Markdown heading extraction because this root represents source-outline metadata, not AI-derived page content. Its `content` must list original Markdown headings with line numbers or state that no clear Markdown headings were found. Do not create any local page, MVC, feature, or document-package nodes from headings.

    Change `runDecompositionJob()` so it initializes/merges the source-outline root, then merges AI page nodes under that root, then expands those page nodes. Preserve the existing fire-and-forget session model, polling shape, heartbeat in `withDecompositionProgress()`, timeout, `MAX_PARALLEL_DECOMPOSITION_BRANCHES`, and natural large-PRD progress behavior. Only adjust step labels if needed to mention pages/MVC.

    Implement D-01 and D-03 in the L1 tool/schema and prompts: top-level AI output should now be page/screen/major UX page nodes, not generic `client/server/config` document-package folders. Force or post-process each L1 AI node so `parentId` points to the source-outline root, `level` is `1`, `docPath` is `null`, and `content` contains original evidence plus concise organization. If Claude accidentally returns `parentId: null`, attach it to the root instead of treating it as another root. This post-processing must not invent node labels/content; it only fixes parent/level metadata.

    Implement D-02 and D-03 in the branch tool/schema and prompts: expanding a page must ask for children representing only non-empty `model`, `ctrl`, and/or `view` slices. Do not require all three. Do not emit placeholder nodes such as “model 暂无内容”. If a category is missing, require a “需澄清” note inside the page node or an existing evidence-backed MVC child. Child `docPath` values should be page-scoped and stable, such as `pages/<page-slug>/model.md`, `pages/<page-slug>/ctrl.md`, `pages/<page-slug>/view.md`, when that slice exists. Keep all user-visible generated text Chinese except IDs, paths, fields, enums, APIs, and product names.

    Update retry/error wording to match page/MVC decomposition. Remove or rewrite old “多文件知识库 docs/prd/client/server/config/api” guidance where it conflicts with the locked decisions. Keep existing anti-template filtering and no-fake-node behavior.

    Update export index logic if needed so “顶层范围速查” lists page nodes under the source-outline root rather than only listing the source root. This keeps export navigation meaningful after D-01 without changing zip binary handling.
  </action>
  <verify>
    <automated>npm run typecheck:server</automated>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('server/index.ts','utf8'); for (const term of ['SOURCE_OUTLINE_ROOT','原文目录','model','ctrl','view','关键原文摘录']) { if (!s.includes(term)) throw new Error('missing '+term) }"</automated>
  </verify>
  <done>
    `server/index.ts` builds a single local source-outline root, attaches AI page nodes beneath it, expands pages into only evidence-backed MVC children, preserves large-PRD progress behavior, and keeps no local fake page nodes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Align map labels with the new root-page-MVC hierarchy</name>
  <files>src/components/map/TreeCanvas.tsx, src/components/map/NodeCard.tsx</files>
  <action>
    Make the smallest UI text changes needed for D-01 so the map no longer describes the new root as a generic document-package root.

    In `TreeCanvas.tsx`, update `layerLabel()` to match the new structural depths: depth 0 should communicate `原文目录`, depth 1 should communicate `页面节点`, depth 2 should communicate `MVC 文档` or equivalent concise Chinese. Keep layout math, zoom behavior, refs, connector drawing, and card sizing unchanged.

    In `NodeCard.tsx`, update the `node.parentId === null` card caption from the old generic top-level wording to `原文目录` or `PRD 原文目录`. Keep the existing card structure, click/double-click disambiguation, `DocumentMiniPreview`, styling, and status badge behavior unchanged. Do not add new animations or redesign the card.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>node -e "const fs=require('fs'); const a=fs.readFileSync('src/components/map/TreeCanvas.tsx','utf8'); const b=fs.readFileSync('src/components/map/NodeCard.tsx','utf8'); if(!a.includes('页面节点') || !a.includes('MVC')) throw new Error('layer labels not updated'); if(!b.includes('原文目录')) throw new Error('root card label not updated')"</automated>
  </verify>
  <done>
    The rendered map labels describe the new hierarchy as 原文目录 -> 页面节点 -> MVC 文档 without changing interaction behavior.
  </done>
</task>

</tasks>

<verification>
Run both automated checks after implementation:

```bash
npm run typecheck:server
npm run typecheck
node -e "const fs=require('fs'); const s=fs.readFileSync('server/index.ts','utf8'); for (const term of ['SOURCE_OUTLINE_ROOT','原文目录','model','ctrl','view','关键原文摘录']) { if (!s.includes(term)) throw new Error('missing '+term) }"
node -e "const fs=require('fs'); const a=fs.readFileSync('src/components/map/TreeCanvas.tsx','utf8'); const b=fs.readFileSync('src/components/map/NodeCard.tsx','utf8'); if(!a.includes('页面节点') || !a.includes('MVC')) throw new Error('layer labels not updated'); if(!b.includes('原文目录')) throw new Error('root card label not updated')"
```

Manual smoke check if an Anthropic key is available: import a PRD and confirm the polling screen still advances while final map shows one 原文目录 root, AI page children, and only non-empty MVC grandchildren.
</verification>

<success_criteria>
- Exactly one local root node represents the source outline; no local fake page nodes are generated from Markdown headings.
- AI page nodes are children of the source-outline root.
- Page branches generate only evidence-backed model/ctrl/view children with quote+organize content.
- Existing decomposition polling/heartbeat/progress behavior remains intact for large PRDs.
- TypeScript checks pass for server and full project.
</success_criteria>

<output>
After completion, create `.planning/quick/260602-f4c-prd-mvc-model-ctrl-view-mvc-mvc/260602-f4c-SUMMARY.md`.
</output>
