# Quick Task 260527-x0n: Web/Tauri Quality Optimization

## Goal

Fix the code-level quality issues found during the architecture review, with priority on preserving Deep Forge polishing results through export and making the app safer to build and run in both Web and Tauri modes.

## Tasks

1. Persist Deep Forge output into the PRD tree
   - Files: `src/store/appStore.ts`, `src/pages/ForgePage.tsx`, `server/index.ts`, `src/lib/api.ts`, `src/types/prdNode.ts`
   - Action: add a node-polish result interface, store node chat history, update node content/tech notes when AI marks a node complete or the user confirms, and treat `needsPolish=false` leaf nodes as export-ready.
   - Verify: polished node content appears in generated ZIP markdown and export gating ignores non-polish leaves.

2. Stabilize decomposition polling and server lifecycle
   - Files: `src/pages/MapPage.tsx`, `server/index.ts`
   - Action: prevent overlapping polls, ignore stale sessions after reset, dedupe session cleanup timers, add MCP/tool-loop timeouts and caps, and sanitize ZIP paths.
   - Verify: TypeScript build passes and stale polling cannot mutate a new upload session.

3. Tighten build, Web/Tauri compatibility, and security posture
   - Files: `package.json`, `tsconfig.node.json`, `src/lib/download.ts`, `src/components/layout/SettingsPanel.tsx`, `src-tauri/tauri.conf.json`, `src-tauri/capabilities/default.json`
   - Action: include server in type checks, add Web download fallback, surface proxy URL dynamically, restore a CSP, and narrow filesystem write scope.
   - Verify: `npm run build`, targeted server typecheck, and relevant static inspections pass.

