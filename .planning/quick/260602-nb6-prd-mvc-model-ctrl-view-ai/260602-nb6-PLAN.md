---
phase: quick-260602-nb6-prd-mvc-model-ctrl-view-ai
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/prdNode.ts
  - server/index.ts
  - src/lib/api.ts
  - src/store/appStore.ts
  - src/pages/ForgePage.tsx
  - src/components/map/ForgeChat.tsx
  - src/components/map/NodeOperationReview.tsx
autonomous: true
requirements:
  - QUICK-260602-NB6
must_haves:
  truths:
    - "PRD decomposition classifies Model/Ctrl/View by factual evidence dimension, not by loose keyword matching."
    - "Nodes or suggestions based only on user/upload supplements are visibly marked as user/upload sourced, not original PRD evidence."
    - "Left-side Forge AI chat can upload or paste supplemental text/resources and returns pending create/update node operations for review."
    - "Supplement upload and AI suggestion generation never mutate the mind map until the user explicitly applies a suggested operation."
  artifacts:
    - path: "src/types/prdNode.ts"
      provides: "MVC audience and node operation suggestion contracts"
      contains: "PrdNodeOperationSuggestion"
    - path: "server/index.ts"
      provides: "Evidence-driven MVC prompts and suggestion endpoint"
      contains: "/api/prd-node-suggestions"
    - path: "src/components/map/ForgeChat.tsx"
      provides: "Left chat supplement upload entry and suggestion review trigger"
      contains: "生成节点建议"
    - path: "src/store/appStore.ts"
      provides: "Pending suggestion state and explicit apply/dismiss actions"
      contains: "applyNodeOperationSuggestion"
  key_links:
    - from: "src/components/map/ForgeChat.tsx"
      to: "src/lib/api.ts"
      via: "onSuggestNodeOperations callback calls suggestPrdNodeOperations"
      pattern: "suggestPrdNodeOperations"
    - from: "server/index.ts"
      to: "src/types/prdNode.ts"
      via: "suggestion response uses PrdNodeOperationSuggestion shape"
      pattern: "PrdNodeOperationSuggestion"
    - from: "src/store/appStore.ts"
      to: "prdTree"
      via: "applyNodeOperationSuggestion mutates tree only after explicit user apply"
      pattern: "applyNodeOperationSuggestion"
---

<objective>
Optimize PRD mind-map MVC splitting and add a review-first supplement flow in the Forge AI chat.

Purpose: Make decomposition and later map adjustments trustworthy: Model/Ctrl/View should come from evidence/fact dimensions, and supplemental user/upload evidence should produce reviewable operations rather than silently rewriting the map.
Output: Updated node contracts, server prompts/API, store actions, and ForgeChat UI for upload-to-suggestion-to-confirm flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/types/prdNode.ts
@server/index.ts
@src/lib/api.ts
@src/store/appStore.ts
@src/pages/ForgePage.tsx
@src/components/map/ForgeChat.tsx

<interfaces>
Current `PrdNode` has stable fields `id`, `parentId`, `label`, `summary`, `content`, `type`, `status`, `level`, `order`, `needsPolish`, `extractedFrom`, `techNotes`, `children`, optional `docPath`, `audience`, `handoffGoal`, `qualityGate`.

Current node chat response in `src/lib/api.ts`:
```ts
export interface NodeChatResponse {
  reply: string
  nodeComplete: boolean
  nodePatch?: {
    summary?: string | null
    content?: string | null
    techNotes?: string | null
  } | null
}
```

Current `ForgeChat` is props-only for mutations: it calls callbacks such as `onSend`, `onConfirm`, `onGeneratePrototype`, `onClearChat`. Preserve that pattern: add suggestion callbacks as props; do not import Zustand into `ForgeChat`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make MVC node classification evidence-driven</name>
  <files>src/types/prdNode.ts, server/index.ts</files>
  <behavior>
    - A rule/config/data fact should classify as Model only when the evidence is about domain data, formulas, config, persistent state, or invariant rules.
    - A flow/API/command/state-transition fact should classify as Ctrl only when the evidence is about control logic, orchestration, validation, request/response, or lifecycle.
    - A UI/layout/copy/animation/feedback fact should classify as View only when the evidence is about presentation or user-facing interaction.
    - Keyword presence alone must not force classification; prompt text must explicitly prohibit keyword-only classification.
    - Original PRD evidence remains `sourceKind: 'prd'`; user/upload-only evidence must not be represented as original PRD evidence.
  </behavior>
  <action>
    Extend `PrdNodeAudience` with `model`, `ctrl`, and `view` while keeping existing values for backward compatibility. Add optional source/provenance fields to `PrdNode` without forcing a storage migration, for example `sourceKind?: 'prd' | 'user' | 'upload'` and `evidenceRefs?: Array<{ sourceKind: 'prd' | 'user' | 'upload'; sourceLabel: string; quote?: string | null }>`.

    In `server/index.ts`, update `decompositionMethodologyGuide`, both decomposition tool schemas, `normalizeAudience`, and `normalizeDecompositionNodes` so generated nodes can carry MVC audience and provenance. Rewrite the prompt rules around lines 704-713 and branch prompt rules around lines 1139-1155 to say MVC is evidence-dimension based, not keyword based. Use these definitions consistently: Model = domain facts/data/config/state/rules; Ctrl = flow orchestration/API/commands/validation/state transitions; View = UI layout/copy/animation/visual feedback. For decomposition from the original PRD, default missing provenance to `sourceKind: 'prd'`; do not create fake `extractedFrom` values when no PRD location is known.

    Do not add dependencies and do not change unrelated prototype/export behavior.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>`PrdNode` supports MVC/provenance, server prompt/schema normalization accepts it, and TypeScript passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add review-only node operation suggestion API</name>
  <files>src/types/prdNode.ts, src/lib/api.ts, server/index.ts</files>
  <behavior>
    - Posting supplemental text/resources to the new API returns suggested operations only; it never mutates `prdTree` or decomposition sessions.
    - Suggestions include operation kind, target/parent, patch, rationale, confidence, and evidence/provenance.
    - If a suggestion is based on supplement/upload content without matching PRD evidence, it is marked `user` or `upload` sourced.
    - Suggestions use the evidence-driven MVC rules from Task 1.
  </behavior>
  <action>
    In `src/types/prdNode.ts`, add a small operation contract such as `PrdNodeOperationSuggestion` with: `id`, `operation: 'create' | 'update'`, `targetNodeId?: string | null`, `parentId?: string | null`, `patch` containing only allowed `PrdNode` fields (`label`, `summary`, `content`, `type`, `needsPolish`, `docPath`, `audience`, `handoffGoal`, `qualityGate`, `techNotes`, `sourceKind`, `evidenceRefs`), `rationale`, `confidence`, `evidenceRefs`, and optional `status: 'pending' | 'applied' | 'dismissed'`. Keep the contract minimal and serializable.

    In `src/lib/api.ts`, export request/response types and `suggestPrdNodeOperations(baseUrl, payload)` that POSTs JSON to `/api/prd-node-suggestions`. Payload should include `tree`, `selectedNodeId`, `supplementText`, and a lightweight `sources` array for uploaded resource names/text snippets; reuse existing `ContentBlock` only if image evidence is sent.

    In `server/index.ts`, add `POST /api/prd-node-suggestions`. Validate `tree` and non-empty supplement/source content. Build an Anthropic prompt/tool that returns at most 5 create/update suggestions and explicitly says: do not directly edit the map; only propose operations for user confirmation; classify MVC by evidence dimension; if original PRD evidence cannot be cited from existing node content/extractedFrom, mark evidence as `user` or `upload`. Normalize the response defensively, dropping malformed operations and defaulting source labels to the uploaded file name or `用户补充`.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Frontend API wrapper and server endpoint compile, return normalized suggestion objects, and contain no code path that mutates `prdTree` before user approval.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Forge chat upload-to-suggestion review UI</name>
  <files>src/store/appStore.ts, src/pages/ForgePage.tsx, src/components/map/ForgeChat.tsx, src/components/map/NodeOperationReview.tsx</files>
  <behavior>
    - The left Forge chat composer exposes a supplement upload/paste path separate from normal node-polish send.
    - Clicking `生成节点建议` calls the suggestion API and displays pending create/update cards.
    - Pending suggestions show operation kind, target/parent, rationale, and source badge (`PRD`, `用户补充`, or `上传资料`).
    - Uploading/analyzing suggestions does not mutate the map; only clicking an explicit apply/confirm button mutates `prdTree`.
    - Dismissing a suggestion removes/marks it without changing any node.
  </behavior>
  <action>
    In `src/store/appStore.ts`, add pending suggestion state and actions: `setNodeOperationSuggestions(scopeId, suggestions)`, `dismissNodeOperationSuggestion(scopeId, suggestionId)`, and `applyNodeOperationSuggestion(scopeId, suggestionId)`. Use current `rebuildPrdTreeLinks` after applying. For `update`, merge only the allowed patch fields into the target node. For `create`, insert the node under `parentId` or the current node, assign a stable generated id only if the suggestion lacks one, set `status: 'pending'`, and preserve provenance. This is the only place where suggestions may mutate `prdTree`.

    In `src/pages/ForgePage.tsx`, import `suggestPrdNodeOperations` and wire an `onSuggestNodeOperations` callback passed to `ForgeChat`. It should gather the current `prdTree`, `nodeId`, pasted supplement text, and upload source metadata, call the API, store returned suggestions under the current node scope, and append a short assistant message summarizing that suggestions are waiting for confirmation. Do not call `applyNodePolish`, `setPrdTree`, or `mergePartialTree` from this callback.

    In `src/components/map/ForgeChat.tsx`, add a compact supplement area in the left chat input section: allow pasting text in the existing draft and uploading `.md`, `.txt`, `.json` resource files as text sources; show pending file chips; add a secondary button labeled `生成节点建议`. Keep the existing `发送` behavior for normal polishing. Create `src/components/map/NodeOperationReview.tsx` to render pending suggestion cards with Apply and Dismiss buttons; `ForgeChat` receives suggestions and callbacks via props and remains props-only. Source badges must distinguish PRD vs user/upload so supplemental evidence is not presented as original PRD.

    Keep the UI surgical and aligned with existing Tailwind/Forge Blueprint classes. Do not add new routes, dependencies, or broad layout refactors.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Forge chat can generate reviewable node operation suggestions from supplemental input, review cards render, apply/dismiss are explicit, and no suggestion changes the map before Apply.</done>
</task>

</tasks>

<verification>
Run `npm run typecheck` after all tasks. If possible, run the app and verify manually: open a Forge node, paste supplemental text, click `生成节点建议`, confirm cards appear, dismiss one card, apply one card, then return to the map and confirm only the applied operation changed the tree.
</verification>

<success_criteria>
- MVC prompt/schema logic is evidence-driven and supports `model`, `ctrl`, `view` without breaking existing audiences.
- User/upload-only supplemental evidence is marked as user/upload source in suggestions and applied nodes.
- Supplement input in Forge chat produces pending operations for review, not direct map mutations.
- TypeScript typecheck passes with no new dependencies.
</success_criteria>

<output>
After completion, create `.planning/quick/260602-nb6-prd-mvc-model-ctrl-view-ai/260602-nb6-SUMMARY.md`.
</output>
