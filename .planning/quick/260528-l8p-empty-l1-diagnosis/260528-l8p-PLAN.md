# 快速任务 260528-l8p 计划

**任务：** 修复 AI 顶层拆分返回空导致 PRD 分析失败
**日期：** 2026-05-28

## 目标

用户导入 PRD 后不应出现“AI 未返回有效顶层文档包目录”的笼统失败。需要判断模型到底没有返回、返回了模板节点、返回了字段不完整节点，还是请求结构导致模型无法稳定给出顶层目录，并据此修复。

## 待办

1. 建立一个最小 PRD 的接口复现循环，确认失败模式。
2. 给服务端顶层拆分结果增加可诊断的安全摘要，避免只返回“空”。
3. 修复顶层拆分的提示/归一化策略，让真实 AI 结果可以进入导图，同时继续拒绝本地模板假节点。
4. 运行构建与本地服务验证。
