# Quick Task 260529-o5r Plan

## Goal

打开游戏初始化时界面最大化，并检查 Deep Forge 打磨界面是否与 `D:\learn\abi-screenshot-to-code` 的主编辑界面模式一致；只做必要的最小修改。

## Tasks

1. **Tauri 启动窗口最大化**
   - Files: `src-tauri/tauri.conf.json`
   - Action: Add the Tauri window `maximized` setting while keeping the existing size/resizable fallback.
   - Verify: Config remains valid JSON and build/typecheck still passes.

2. **Deep Forge 渲染对齐检查与最小修正**
   - Files: `src/pages/ForgePage.tsx`, `src/components/map/ForgeChat.tsx`, `src/components/state/PrototypeBoard.tsx`
   - Action: Compare current layout with `D:\learn\abi-screenshot-to-code\frontend\src\App.tsx` and `components/preview/PreviewPane.tsx`: left content panel + large right preview with preview/code tabs. Apply only minimal layout changes if current Forge view prevents the same dominant preview/editor pattern.
   - Verify: Typecheck/build passes; inspect the rendered Forge route in browser if dev server can run.

3. **GSD artifacts and state**
   - Files: `.planning/quick/260529-o5r-d-learn-abi-screenshot-to-code/260529-o5r-SUMMARY.md`, `.planning/STATE.md`
   - Action: Record comparison findings, changes, and validation result; append quick task row.
   - Verify: Summary exists and STATE quick task table includes this task.
