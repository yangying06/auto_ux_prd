# 快速任务 260528-k8w 计划

**任务：** 修复导入后 PRD 分析进度重复且最终失败
**日期：** 2026-05-28

## 目标

导入 PRD 后，进度列表不能把同一个 AI 阶段重复展示；后端不能因为顶层或分支输出过长、偶发空数组而让整次分析失败。

## 任务

1. 复现导入失败和重复进度，确认前端进度归一化与后端拆分调用的真实根因。
2. 收紧顶层和分支拆分协议，避免 Claude 工具输出被 `max_tokens` 截断，并为偶发空数组增加 AI 重试。
3. 调整轮询进度为同阶段更新和并发分支聚合展示。
4. 运行类型检查、构建、真实 PRD 轮询和浏览器页面验证。

