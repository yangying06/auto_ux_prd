---
must_haves:
  truths:
    - Prototype generation accepts create and update inputs.
    - Generated or updated HTML is normalized before rendering.
    - Preview rendering is isolated from the app shell.
    - Users can restore prior prototype versions.
    - Users can send a structured spec prompt to bolt.new.
  artifacts:
    - src/lib/prototypeUtils.ts
    - src/lib/specPrompt.ts
    - public/sandbox.html
    - server/index.ts
    - src/store/appStore.ts
    - src/components/state/PrototypeBoard.tsx
    - src/components/state/StateCanvas.tsx
    - src/components/layout/AppShell.tsx
    - src/components/map/TopAppBar.tsx
    - src/pages/MapPage.tsx
  key_links:
    - docs/ai-prototype-integration-proposal.md
---

# Quick Task 260527-sl3 Plan

## Task 1: Prototype HTML Pipeline

files:
- `src/lib/prototypeUtils.ts`
- `public/sandbox.html`
- `src/components/state/PrototypeBoard.tsx`

action:
- Add HTML extraction and wrapping helpers that tolerate Markdown prose, fenced blocks, partial documents, and missing Tailwind CDN.
- Render prototypes through a sandbox host page using `postMessage` hydration instead of direct app-owned `srcDoc`.
- Keep the board layout stable while adding compact iteration and history controls.

verify:
- PrototypeBoard compiles with strict TypeScript.
- Empty, loading, raw HTML, and Markdown-fenced HTML paths are handled.

done:
- Browser preview is sandboxed and normalized HTML is displayed.

## Task 2: Create/Update Prototype API

files:
- `server/index.ts`
- `src/lib/api.ts`
- `src/components/layout/AppShell.tsx`

action:
- Extend `/api/prototype` to accept `currentHtml` and `instruction`.
- Use stronger single-file prototype generation prompts.
- Add `edit_prototype` tool handling for targeted string replacement, with fallback to whole-document HTML.
- Send current prototype HTML from the frontend only when the user provides an iteration instruction.

verify:
- Request/response types compile.
- Existing one-click generation still works.
- Update path returns valid HTML even if exact replacement fails and the model returns a full document.

done:
- Create and update prototype routes are both wired.

## Task 3: Prototype History And Rollback

files:
- `src/store/appStore.ts`
- `src/components/state/StateCanvas.tsx`
- `src/components/state/PrototypeBoard.tsx`

action:
- Add bounded prototype history entries with timestamps, labels, mode, and note.
- Persist current prototype and history by bumping the Zustand storage version.
- Provide restore controls in the prototype board.

verify:
- Migration preserves existing v4 project state.
- Reset actions clear prototype and history.

done:
- Users can see and restore recent prototype versions.

## Task 4: bolt.new Validation Exit

files:
- `src/lib/specPrompt.ts`
- `src/components/state/StateCanvas.tsx`
- `src/components/layout/AppShell.tsx`
- `src/components/map/TopAppBar.tsx`
- `src/pages/MapPage.tsx`

action:
- Build structured prompt generation for `UXRequirementState` and `PrdTree`.
- Add buttons that open `https://bolt.new/?prompt=...` from the forged requirement board and map/export toolbar.
- Keep this as an external validation exit, not a local WebContainer integration.

verify:
- URL opens from user click handlers.
- Prompt includes enough structure to generate a runnable HTML prototype.

done:
- Both requirement and exported-tree flows have a runnable prototype validation exit.

## Task 5: Build Verification And GSD Closeout

files:
- `.planning/quick/260527-sl3-docs-ai-prototype-integration-proposal-m/260527-sl3-SUMMARY.md`
- `.planning/quick/260527-sl3-docs-ai-prototype-integration-proposal-m/260527-sl3-VERIFICATION.md`
- `.planning/STATE.md`

action:
- Run `npm run build`.
- Write summary and verification artifacts.
- Update `.planning/STATE.md` quick task table.

verify:
- Build passes or any residual failure is documented with exact cause.

done:
- GSD artifacts reflect completion.
