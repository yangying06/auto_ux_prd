# 快速任务 260528-k4m 计划

**任务：** 修复导入后分析进度看似卡死
**日期：** 2026-05-28

## 目标

导入 PRD 后，即使不能再展示本地假节点，也要让用户看到真实的 AI 分析进度；如果 AI 调用长时间没有返回，必须给出明确失败原因，而不是一直停留在“正在分析PRD文档”。

## 待办

1. 给 PRD 拆分的 Claude 调用增加阶段心跳，定期更新 `currentStep`。
2. 给顶层拆分和分支拆分增加超时保护，超时后进入错误态。
3. 调整前端进度文案，让 0 节点时显示“等待 AI 返回首批文档包”，而不是误导成已经发现 0 个节点。
4. 运行构建，并启动本地服务验证页面可访问。
