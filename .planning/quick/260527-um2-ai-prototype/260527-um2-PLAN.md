---
must_haves:
  truths:
    - Existing AI prototype proposal integration has create/update prototype routing, sandbox rendering, history restore, and bolt validation already implemented.
    - Mind map layout groups descendants under their parent and reduces cross-branch mixing.
    - Deep Forge polishing page is node-specific again and supports image reference upload.
    - Node chat API forwards uploaded images to Claude instead of reducing them to text.
  artifacts:
    - src/components/map/TreeCanvas.tsx
    - src/components/map/NodeCard.tsx
    - src/pages/ForgePage.tsx
    - src/components/map/ForgeChat.tsx
    - src/components/map/ForgeNodePanel.tsx
    - server/index.ts
---

# Quick Task 260527-um2 Plan

## Task 1: Proposal Completion Audit

files:
- `docs/ai-prototype-integration-proposal.md`
- `.planning/quick/260527-sl3-docs-ai-prototype-integration-proposal-m/260527-sl3-SUMMARY.md`
- existing implementation files touched by 260527-sl3

action:
- Verify which proposal phases are implemented.
- Treat remaining gaps as this quick task scope instead of reverting existing work.

verify:
- Summary explicitly calls out completed proposal items and this iteration's remaining additions.

done:
- Existing prototype integration is considered complete enough for its documented P0/P1/P2 scope; new work focuses on mind map hierarchy and reference-image polishing.

## Task 2: Mind Map Hierarchy Layout

files:
- `src/components/map/TreeCanvas.tsx`
- `src/components/map/NodeCard.tsx`

action:
- Replace flat flex-column-per-level distribution with a measured tree layout.
- Center parent cards against their descendant blocks and render connector paths from computed positions.
- Add stable card dimensions and layer labels so root/module/feature/ui depth reads clearly.

verify:
- TypeScript compile passes.
- Map viewport still supports pan/zoom and selected card interaction.

done:
- Sibling groups stay visually clustered under the same parent.

## Task 3: Reference-Image Deep Forge

files:
- `src/pages/ForgePage.tsx`
- `src/components/map/ForgeChat.tsx`
- `src/components/map/ForgeNodePanel.tsx`
- `src/lib/api.ts`
- `server/index.ts`

action:
- Restore ForgePage to the node-specific Deep Forge layout instead of generic AppShell.
- Add image-only reference upload, preview tray, reference/asset mode labels, and quick visual-analysis prompts to ForgeChat.
- Send mixed text+image content through nodeChats and `/api/node-chat`.
- Update server node-chat route to pass user image blocks to Anthropic with screenshot/reference analysis instructions.

verify:
- TypeScript compile and production build pass.
- Browser smoke test opens map/forge path and confirms the page renders the reference upload controls.

done:
- Designers can upload screenshots/reference images during node polishing and the model receives those images.

## Task 4: Closeout

files:
- `.planning/quick/260527-um2-ai-prototype/260527-um2-SUMMARY.md`
- `.planning/quick/260527-um2-ai-prototype/260527-um2-VERIFICATION.md`
- `.planning/STATE.md`

action:
- Run verification.
- Write summary and verification artifacts.
- Update GSD state quick task table.

verify:
- GSD artifacts exist and describe the iteration.

done:
- Work is closed with evidence.
