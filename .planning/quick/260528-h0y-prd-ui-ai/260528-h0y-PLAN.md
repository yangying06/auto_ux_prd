# Quick Task 260528-h0y Plan

**Task:** 将 PRD 拆分从 UI 交互节点树调整为 AI 可接力执行的多文件文档包方法论
**Date:** 2026-05-28

## Intent

当前拆分更像功能/控件导图，不能稳定产出“一篇篇交给 AI 继续做事”的 Markdown 文档包。本次只做小范围修正：保留现有导图 UI 和 Zustand 结构，但把服务端拆解语义、节点元数据、Deep Forge 打磨目标和 zip 导出格式改成“AI 接力文档包”。

## Tasks

1. Update decomposition schema and prompts in `server/index.ts`
   - Treat leaves as Markdown document packets with `docPath`, target audience, AI handoff goal, source evidence and quality gates.
   - Bias top-level grouping toward overview/gameplay/client/server/config/api/acceptance/appendix rather than UI controls.

2. Preserve document metadata through normalization and export
   - Add optional metadata fields to `PrdNode`.
   - Export leaves to stable methodology-style paths when `docPath` is present.
   - Generate a synthetic `00-INDEX.md` with role navigation and exported file list.

3. Retune Deep Forge copy and prompt
   - Make polishing work for any document packet, not only UI interaction nodes.
   - Keep reference-image handling for UI/client documents.

## Verification

- Run TypeScript/build checks.
- Inspect key prompt/export code paths for compatibility with existing UI.
