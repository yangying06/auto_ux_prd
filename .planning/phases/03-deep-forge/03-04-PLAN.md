---
phase: 03-deep-forge
plan: 04
type: execute
wave: 3
depends_on:
  - 03-03
files_modified: []
autonomous: false
requirements:
  - FORG-01
  - FORG-02
  - FORG-03
  - FORG-04
  - FORG-05
  - FORG-06
  - FORG-07

must_haves:
  truths:
    - "User can open any node from the map's PreviewDrawer and land on the Deep Forge view"
    - "Left panel shows node ID, type, title, summary, techNotes (if present), status"
    - "Chat area shows auto-welcome message on first open; each node's history is independent"
    - "Sending a message calls /api/node-chat and the AI reply appears in the chat"
    - "When AI judges requirements complete, Confirm Complete button highlights with active-glow"
    - "Clicking Confirm Complete updates node status to 'done' and navigates back to map"
    - "Map shows 'Generated' badge on the confirmed node"
  artifacts:
    - path: "src/pages/ForgePage.tsx"
      provides: "Full Deep Forge page"
    - path: "src/components/map/ForgeNodePanel.tsx"
      provides: "Node detail panel"
    - path: "src/components/map/ForgeChat.tsx"
      provides: "Chat panel"
    - path: "src/store/appStore.ts"
      provides: "nodeChats, appendNodeMessage, clearNodeChat, updateNodeStatus"
    - path: "server/index.ts"
      provides: "/api/node-chat endpoint"
  key_links:
    - from: "PreviewDrawer Enter Deep Forge button"
      to: "ForgePage /forge/:nodeId"
      via: "wouter navigate"
      pattern: "forge"
    - from: "ForgePage handleConfirm"
      to: "MapPage NodeCard status badge"
      via: "Zustand reactivity (prdTree update)"
      pattern: "updateNodeStatus"
---

<objective>
Human integration verification for the complete Deep Forge workflow. Verify that the end-to-end flow from map → forge → chat → confirm → map works correctly with the real AI backend running.

Purpose: This is the only checkpoint in Phase 3. All prior plans are autonomous; this plan ensures the assembled system works from the user's perspective.
Output: Confirmed working Deep Forge session with AI responses and node status updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-deep-forge/03-CONTEXT.md
@.planning/phases/03-deep-forge/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run TypeScript check and fix any errors</name>
  <files>src/pages/ForgePage.tsx, src/components/map/ForgeChat.tsx, src/components/map/ForgeNodePanel.tsx, src/store/appStore.ts, src/lib/api.ts, server/index.ts</files>
  <read_first>
    - src/pages/ForgePage.tsx — verify imports and structure are correct
    - src/store/appStore.ts — verify nodeChats, appendNodeMessage, updateNodeStatus are present
    - src/lib/api.ts — verify sendNodeChatMessage is exported
  </read_first>
  <action>
Run the full TypeScript compile check across all targets:

```bash
npx tsc -p tsconfig.app.json --noEmit
npx tsc -p tsconfig.node.json --noEmit
```

Both must pass with zero errors before proceeding to the checkpoint.

If any TypeScript errors appear, fix them before handing off to the human verification step. Common issues to check:
- ForgeNodePanel or ForgeChat accidentally importing from appStore
- Mismatched prop types between ForgePage and the components
- Missing import for `useAppStore` in ForgePage
- `sendNodeChatMessage` import path incorrect in ForgePage
- `useEffect` missing `appendNodeMessage` in deps array causing lint/TS error

Fix any errors in the relevant files from Plans 01-03 and re-run until clean.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.app.json --noEmit && npx tsc -p tsconfig.node.json --noEmit && echo "TypeScript clean"</automated>
  </verify>
  <acceptance_criteria>
    - Both tsc commands exit with code 0
    - Output contains "TypeScript clean" (no error messages)
  </acceptance_criteria>
  <done>Zero TypeScript errors across frontend and server targets.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human integration verification of Deep Forge workflow</name>
  <files></files>
  <action>Human verifies the end-to-end Deep Forge workflow following the steps in how-to-verify. No code changes required in this task — verification only.</action>
  <verify>Human approval via resume-signal.</verify>
  <done>Human types "approved" after verifying all 8 steps pass.</done>
  <what-built>
Complete Deep Forge workflow:
- ForgePage replaces the placeholder stub at /forge/:nodeId
- Left panel: node ID badge, type badge, title, summary, techNotes (if present), status badge
- Right panel: AI chat with auto-welcome, message bubbles, input + send, Confirm Complete button
- Server /api/node-chat endpoint with node context injection
- Confirmation flow: button → updateNodeStatus('done') → navigate to map → 'Generated' badge visible
  </what-built>
  <how-to-verify>
Pre-conditions: app running (`npm run dev`), API key set in .env, a PRD has been uploaded and decomposed.

Step 1 — Navigate to forge view:
- From the map view, click any leaf node (type='ui' preferred, has needsPolish=true)
- PreviewDrawer opens on the right
- Click "Enter Deep Forge" button
- Expected: /forge/:nodeId loads with two-column layout (NOT the old placeholder)

Step 2 — Verify left panel (ForgeNodePanel):
- Left panel (~360px) shows: type badge (module/feature/ui), ID badge (e.g. "CE-01"), node title, summary text
- If node has techNotes: verify a "技术备注" section appears
- Status badge shows "待处理" (pending)

Step 3 — Verify welcome message:
- Right panel shows auto-welcome: "正在为节点 {label}（{id}）开启深度打磨..."
- No duplicate welcome messages (StrictMode guard working)

Step 4 — Send a message:
- Type a message about the node's interaction details, press Enter or Send
- Loading dots appear while waiting
- AI reply appears as assistant bubble
- Reply is in Chinese, concise (no more than 8 lines), asks a focused question

Step 5 — Verify per-node independence:
- Click "返回导图" (Back to Map)
- Enter a DIFFERENT node's forge view
- Chat should be empty (new auto-welcome only) — no crossover from the first node

Step 6 — AI completion signal:
- In a forge session, send enough detail that the AI is satisfied
- Expected: AI reply includes no visible JSON, but Confirm Complete button changes to green glow
- If AI doesn't signal in one session, this can be skipped — the button visual state may require more conversation

Step 7 — Confirm and navigate:
- Click "确认完成" button
- Expected: immediately navigates back to map view
- Verify: the confirmed node's card now shows "Generated" badge (green, not orange "To Process")

Step 8 — Re-enter the confirmed node:
- Click the confirmed node → PreviewDrawer → Enter Deep Forge
- Left panel status badge should show "已完成"
- Chat history from the previous session should still be there (session persists within the browser tab)
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass, or describe specific issues (step number + what happened vs expected)</resume-signal>
</task>

</tasks>

<verification>
Phase 3 complete when:
1. TypeScript clean (zero errors)
2. Human verification approved for all 8 steps above
3. FORG-01 through FORG-07 behaviorally confirmed:
   - FORG-01: Per-node chat history (Step 5)
   - FORG-02: Props-only components (TypeScript check)
   - FORG-03: AI completion signal highlights button (Step 6)
   - FORG-04: Manual confirm works at any time (Step 7)
   - FORG-05: Confirmed node navigates to map (Step 7)
   - FORG-06: Parent context auto-injected in server (Step 4 — AI has parent context)
   - FORG-07: /api/node-chat endpoint injects node content (Step 4)
</verification>

<success_criteria>
- User can complete a full forge session: enter node → chat with AI → confirm → map shows "Generated" badge
- Each node's chat history is independent
- AI responds in Chinese with contextual questions about the specific node
- Confirm Complete button highlights when AI signals nodeComplete=true
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-deep-forge/03-04-SUMMARY.md`
</output>
